import(methods)
import(AnnotationDbi)

### Only put what is statically exported here. All the AnnObj instances
### created at load time are dynamically exported (refer to R/zzz.R for
### the details).
export(
    org.Hvulgare.eg,
    org.Hvulgare.eg_dbconn,
    org.Hvulgare.eg_dbfile,
    org.Hvulgare.eg_dbschema,
    org.Hvulgare.eg_dbInfo,

    org.Hvulgare.egORGANISM
)

